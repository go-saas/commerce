groups:

  - name: "permission.ticketing.location.group"
    def:
      - name: "permission.ticketing.location.any"
        namespace: "ticketing.location"
        action: "*"
        internal: true
      - name: "permission.permission.read"
        namespace: "ticketing.location"
        action: "read"
      - name: "permission.permission.write"
        namespace: "ticketing.location"
        action: "write"
  - name: "permission.ticketing.activity.group"
    def:
      - name: "permission.ticketing.activity.any"
        namespace: "ticketing.activity"
        action: "*"
        internal: true
      - name: "permission.permission.read"
        namespace: "ticketing.activity"
        action: "read"
      - name: "permission.permission.write"
        namespace: "ticketing.activity"
        action: "write"

  - name: "permission.ticketing.show.group"
    def:
      - name: "permission.ticketing.show.any"
        namespace: "ticketing.show"
        action: "*"
        internal: true
      - name: "permission.permission.read"
        namespace: "ticketing.show"
        action: "read"
      - name: "permission.permission.write"
        namespace: "ticketing.show"
        action: "write"
      - name: "permission.permission.recommend"
        namespace: "ticketing.show"
        action: "recommend"
        side: HOST_ONLY
  - name: "permission.ticketing.ticket.group"
    def:
      - name: "permission.ticketing.ticket.any"
        namespace: "ticketing.ticket"
        action: "*"
        internal: true
      - name: "permission.permission.read"
        namespace: "ticketing.ticket"
        action: "read"
      - name: "permission.permission.write"
        namespace: "ticketing.ticket"
        action: "write"

  - name: "permission.ticketing.category.group"
    def:
      - name: "permission.ticketing.category.any"
        namespace: "ticketing.category"
        action: "*"
        internal: true
        side: HOST_ONLY
      - name: "permission.permission.read"
        namespace: "ticketing.category"
        action: "read"
      - name: "permission.permission.write"
        namespace: "ticketing.category"
        action: "write"
        side: HOST_ONLY

  - name: "permission.ticketing.banner.group"
    side: HOST_ONLY
    def:
      - name: "permission.ticketing.banner.any"
        namespace: "ticketing.banner"
        action: "*"
        internal: true
      - name: "permission.permission.read"
        namespace: "ticketing.banner"
        action: "read"
      - name: "permission.permission.write"
        namespace: "ticketing.banner"
        action: "write"